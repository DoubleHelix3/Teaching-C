// this tells the program to include the standard library (std) input and output (io) functions
// this is what allows you to use printf
#include <stdio.h>

// this is the function that the program will run first when you execute it
// it returns an int based on whether the program was able to run sucessfully or not
int main() {
  printf("Hello World\n");

  // the program was sucessful
  return 0;
}
